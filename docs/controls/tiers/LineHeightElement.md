# LineHeightElement

The height of a text line, relative to the font's own - the tier `Label` and `TextSpan` both wear.

Declared in `lib/StateUI/Sources/Views/LineHeightElement.swift`.

Worn by: [Label](../Label.md)

How each of them realizes these members is in its own file.

| Member | Kind |
| --- | --- |
| `lineHeight` | property |
