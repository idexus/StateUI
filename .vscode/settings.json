{
  // SourceKit resolves imports through SwiftPM manifests, and this repository
  // has several: the library at the root, the AppKit host, one per app.
  "swift.searchSubfoldersForPackages": true,
  "swift.autoGenerateLaunchConfigurations": false,

  // NO swift.swiftEnvironmentVariables HERE. The editor's host - which of
  // STATEUI_APPKIT and its siblings SourceKit-LSP starts with - is chosen in the
  // StateUI extension's status bar (lib/StateUI.VSCode), which sets it on the
  // extension host's own environment. A value here is laid OVER that and would
  // decide the host whatever is chosen.

  // The solution the C# extension and ".NET MAUI: Select Startup Project" read.
  "dotnet.defaultSolution": "StateUI.slnx",

  "files.associations": {
    "*.swift": "swift"
  },
  "files.exclude": {
    "**/bin": true,
    "**/obj": true,
    "**/.build": true
  }
}
